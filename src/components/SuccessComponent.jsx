import { Alert } from "react-bootstrap";

const SuccessComponent = () => {
  return <Alert variant="success position-fixed success-alert">Operazione avvenuta con successo!</Alert>;
};

export default SuccessComponent;
